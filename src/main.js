// Entry for Vite build. Import the SPA module so that bundler includes nostr-tools.
import '../assets/js/spa-nostr-app.js';

// Nothing to export — the SPA script registers event listeners on DOMContentLoaded.
