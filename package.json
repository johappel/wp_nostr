{
  "name": "wp-nostr-spa-build",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir ../assets/js && node ./scripts/postbuild.js",
    "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "nostr-tools": "^1.14.0"
  }
}
